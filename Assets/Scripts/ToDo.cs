using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ToDo : MonoBehaviour
{
    /*
     * -Country Worker schaltet isActive aus, wenn TimeManager.instance.Date über ActiveDuration + Date ist
     * -Measurement Aktivieren sowie Deaktivieren in Country.cs, bei jedem Aufruf kommt InvokeMeasurement
     */
}
